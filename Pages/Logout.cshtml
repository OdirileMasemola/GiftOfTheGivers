@*
    Logout has no visible UI. The code-behind signs the user out and
    redirects home, so this page only needs a route and a title.
    Linked from the top nav, footer, and dashboard Sign Out button.
*@
@page "/Logout"
@model GiftOfTheGivers.Pages.LogoutModel
@{
    ViewData["Title"] = "Logout";
}
